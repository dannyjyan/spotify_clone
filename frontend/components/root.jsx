import React from 'react';
import ReactDOM from 'react-dom';

const Root = ({store}) => (
    <>
        <marquee>REACT WORKS</marquee>
        <h2>store: {store}</h2>
    </>  
);

export default Root;